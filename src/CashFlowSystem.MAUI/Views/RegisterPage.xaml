<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CashFlowSystem.MAUI.ViewModels"
             x:Class="CashFlowSystem.MAUI.Views.RegisterPage"
             x:DataType="viewmodels:RegisterViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Primary}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo/Title -->
            <Label Text="💰" FontSize="50" HorizontalOptions="Center" />
            <Label Text="Crear Cuenta Nueva"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="White" />

            <!-- Register Form Card -->
            <Border BackgroundColor="White"
                    StrokeThickness="0"
                    Padding="20"
                    Margin="0,20,0,0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="15">
                    <Label Text="Registro de Usuario"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}" />

                    <!-- Username -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Nombre de Usuario" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding Username}"
                               Placeholder="Ingrese nombre de usuario"
                               ReturnType="Next" />
                    </VerticalStackLayout>

                    <!-- Email -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Correo Electrónico" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding Email}"
                               Placeholder="ejemplo@correo.com"
                               Keyboard="Email"
                               ReturnType="Next" />
                    </VerticalStackLayout>

                    <!-- Password -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Contraseña" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding Password}"
                               Placeholder="Mínimo 6 caracteres"
                               IsPassword="True"
                               ReturnType="Next" />
                    </VerticalStackLayout>

                    <!-- Confirm Password -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Confirmar Contraseña" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding ConfirmPassword}"
                               Placeholder="Repita la contraseña"
                               IsPassword="True"
                               ReturnType="Done"
                               ReturnCommand="{Binding RegisterCommand}" />
                    </VerticalStackLayout>

                    <!-- Register Button -->
                    <Button Text="Registrarse"
                            Command="{Binding RegisterCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            Margin="0,10,0,0"
                            HeightRequest="50"
                            CornerRadius="10"
                            FontAttributes="Bold" />

                    <!-- Loading Indicator -->
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                     IsVisible="{Binding IsBusy}"
                                     Color="{StaticResource Primary}"
                                     HeightRequest="30" />

                    <!-- Back to Login Link -->
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10,0,0">
                        <Label Text="¿Ya tienes cuenta?" TextColor="{StaticResource Gray600}" />
                        <Label Text="Inicia sesión">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateToLoginCommand}" />
                            </Label.GestureRecognizers>
                            <Label.TextColor>
                                <StaticResource ResourceKey="Primary" />
                            </Label.TextColor>
                            <Label.TextDecorations>Underline</Label.TextDecorations>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
