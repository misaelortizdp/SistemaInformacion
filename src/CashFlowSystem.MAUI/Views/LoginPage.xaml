<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CashFlowSystem.MAUI.ViewModels"
             x:Class="CashFlowSystem.MAUI.Views.LoginPage"
             x:DataType="viewmodels:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Primary}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo/Title -->
            <Label Text="💰" FontSize="60" HorizontalOptions="Center" />
            <Label Text="Sistema de Flujo de Caja"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="White" />
            <Label Text="Gestiona tu negocio de forma profesional"
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="White"
                   Opacity="0.8"
                   Margin="0,0,0,30" />

            <!-- Login Form Card -->
            <Border BackgroundColor="White"
                    StrokeThickness="0"
                    Padding="20"
                    Margin="0,20,0,0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="15">
                    <Label Text="Iniciar Sesión"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}" />

                    <!-- Email -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Correo Electrónico" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding Email}"
                               Placeholder="ejemplo@correo.com"
                               Keyboard="Email"
                               ReturnType="Next" />
                    </VerticalStackLayout>

                    <!-- Password -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Contraseña" TextColor="{StaticResource Gray900}" />
                        <Entry Text="{Binding Password}"
                               Placeholder="Ingrese su contraseña"
                               IsPassword="True"
                               ReturnType="Done"
                               ReturnCommand="{Binding LoginCommand}" />
                    </VerticalStackLayout>

                    <!-- Login Button -->
                    <Button Text="Iniciar Sesión"
                            Command="{Binding LoginCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            Margin="0,10,0,0"
                            HeightRequest="50"
                            CornerRadius="10"
                            FontAttributes="Bold" />

                    <!-- Loading Indicator -->
                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                     IsVisible="{Binding IsBusy}"
                                     Color="{StaticResource Primary}"
                                     HeightRequest="30" />

                    <!-- Register Link -->
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10,0,0">
                        <Label Text="¿No tienes cuenta?" TextColor="{StaticResource Gray600}" />
                        <Label Text="Regístrate aquí">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NavigateToRegisterCommand}" />
                            </Label.GestureRecognizers>
                            <Label.TextColor>
                                <StaticResource ResourceKey="Primary" />
                            </Label.TextColor>
                            <Label.TextDecorations>Underline</Label.TextDecorations>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
