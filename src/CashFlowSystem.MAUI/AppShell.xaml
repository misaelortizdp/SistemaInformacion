<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CashFlowSystem.MAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:CashFlowSystem.MAUI.Views"
    Shell.FlyoutBehavior="Disabled">

    <!-- Login -->
    <ShellContent
        Route="login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Shell.NavBarIsVisible="False" />

    <!-- Register -->
    <ShellContent
        Route="register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Shell.NavBarIsVisible="False" />

    <!-- Main App -->
    <TabBar Route="main">
        <ShellContent
            Route="dashboard"
            Title="Dashboard"
            Icon="home.png"
            ContentTemplate="{DataTemplate views:DashboardPage}" />

        <ShellContent
            Route="transactions"
            Title="Transacciones"
            Icon="list.png"
            ContentTemplate="{DataTemplate views:TransactionsPage}" />

        <ShellContent
            Route="reports"
            Title="Informes"
            Icon="chart.png"
            ContentTemplate="{DataTemplate views:ReportsPage}" />

        <ShellContent
            Route="cashregister"
            Title="Caja"
            Icon="cash.png"
            ContentTemplate="{DataTemplate views:CashRegisterPage}" />
    </TabBar>

</Shell>
